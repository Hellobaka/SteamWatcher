<Window
    x:Class="me.cqp.luohuaming.SteamWatcher.UI.EditBindingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:me.cqp.luohuaming.SteamWatcher.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="编辑绑定"
    Width="400"
    Height="190"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,5"
            Text="QQ号:" />
        <TextBox
            x:Name="QQTextBox"
            Grid.Row="1"
            Margin="0,0,0,10" />

        <TextBlock
            Grid.Row="2"
            Margin="0,0,0,5"
            Text="SteamID:" />
        <TextBox
            x:Name="SteamIDTextBox"
            Grid.Row="3"
            Margin="0,0,0,10" />

        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="OKButton"
                Width="80"
                Margin="0,0,10,0"
                Click="OKButton_Click"
                Content="确定" />
            <Button
                x:Name="CancelButton"
                Width="80"
                Click="CancelButton_Click"
                Content="取消" />
        </StackPanel>
    </Grid>
</Window>